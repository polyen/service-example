import React from 'react';
import { Row, Col, Typography } from 'framework/library';

const Home = () => (
  <div>
    <Row justifyContent="center" className='mt-4'>
      <Col>
        <Typography variant='h1'>Home Page</Typography>
      </Col>
    </Row>
  </div>);

export default Home;